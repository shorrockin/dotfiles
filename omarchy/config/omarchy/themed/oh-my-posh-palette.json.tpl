{
  "_comment": "Rendered by omarchy-theme-set-templates on every `omarchy theme set` to ~/.local/state/omarchy/current/theme/oh-my-posh-palette.json. The theme-set.d/20-oh-my-posh hook then splices this palette into a copy of ~/.config/oh-my-posh/config.json. Only tokens that every Omarchy theme is guaranteed to define are used here (same set as omarchy's own ghostty.conf.tpl).",
  "palette": {
    "os": "{{ muted }}",
    "closer": "p:os",
    "pink": "{{ bright_magenta }}",
    "lavender": "{{ bright_blue }}",
    "blue": "{{ blue }}",
    "green": "{{ green }}",
    "yellow": "{{ yellow }}",
    "red": "{{ red }}",
    "sky": "{{ bright_cyan }}",
    "mauve": "{{ magenta }}",
    "peach": "{{ bright_yellow }}",
    "text": "{{ foreground }}"
  }
}
