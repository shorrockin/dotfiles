# Suspend after one hour without keyboard, pointer, or touch activity.
# Hypridle honors Wayland, D-Bus, and systemd idle inhibitors by default, so
# fullscreen games and applications playing media can keep the system awake.
listener {
    timeout = 3600
    on-timeout = systemctl suspend
}
