# Agent Instructions

Personal, cross-project instructions for coding agents (Claude Code and any
other AGENTS.md-consuming tool), deployed to every machine via this repo's
`common/dots/dot-agents/` package.

This is distinct from any individual project's own CLAUDE.md/AGENTS.md,
including this dotfiles repo's own — see the repo root `CLAUDE.md` for that.

(Placeholder — fill in with actual global preferences/skills as they're written.)
