#!/usr/bin/env bash

WALLPAPER_DIR="$HOME/.config/assets/wallpapers/"
CACHE_FILE="$HOME/.cache/current_wallpaper"

export DISPLAY=:0
export XDG_RUNTIME_DIR=/run/user/$(id -u)
export WAYLAND_DISPLAY=wayland-1

# Get current wallpaper from cache (if exists)
CURRENT_WALL=""
if [[ -f "$CACHE_FILE" ]]; then
    CURRENT_WALL=$(cat "$CACHE_FILE")
fi

# Get a random wallpaper that is not the current one
WALLPAPER=$(find "$WALLPAPER_DIR" -type f ! -path "$CURRENT_WALL" | shuf -n 1)

# Save the selected wallpaper path
echo "$WALLPAPER" > "$CACHE_FILE"

# Create symlink for hyprlock to use
ln -sf "$WALLPAPER" "$HOME/.cache/current_wallpaper.png"

# swaybg renders ~/.cache/current_wallpaper.png (the symlink updated above) and
# is managed as a systemd user service. Restarting it picks up the new image.
systemctl --user restart swaybg
