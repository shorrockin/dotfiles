# Generic Linux

This profile is for headless Linux machines that are not NixOS or Omarchy. Its `dots/` and `config/` entries link to the shared files that belong on those machines.

Add a shared component by linking it from `linux/config/` or `linux/dots/`. Keep machine-specific files out of this profile.
